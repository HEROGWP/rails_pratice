<h2>edit attendee</h2>
<%= form_for @attendee, :url => event_attendee_path(@event, @attendee), :method => :patch do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit "edit attendee", :class => "btn btn-info" %>

<% end %>