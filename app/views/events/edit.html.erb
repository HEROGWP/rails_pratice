<h1>Edit event</h1>
<%= form_for @event, :url => {:controller => :events, :action => :update, :id => @event.id } do |f|%>
	<%= render :partial => "form", :locals => {:f => f} %>

	<%= f.submit "Update"%>
<% end %>